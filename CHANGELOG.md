# Changelog

## Unreleased

- 输入白名单新增 `dash_up/down/left/right` 组合动作，在同一帧同时注入方向与游戏的 Shift/冲刺键；新增可重复的 `text_skip`，对应游戏的 Ctrl 文本快进。长距离移动和重复战斗结算不再需要模拟前台键盘。
- `BridgeRuntime/frame_rate.txt` 可选地把独立 MCP 副本加速到 30–120 FPS；删除文件恢复启动时帧率。当前有效帧率随状态快照返回，脚本不会执行文件中的代码，也不会修改游戏数值或剧情开关。
- 战斗胜利阶段即使仍停留在 `Scene_Battle`，也会根据可见敌人全部阵亡主动推进结算，避免每场胜利空等完整超时。
- 启动前核对仍存活进程的可执行文件路径；桥接暂时掉线时拒绝再启动第二个游戏实例，避免两个进程同时写同一个 `BridgeRuntime`。
- 强制结束游戏前在同一个 PowerShell 进程内核对 PID 对应的可执行文件路径，防止旧快照中的 PID 被其他程序复用后误杀无关进程。
- MCP smoke test 使用隔离的临时运行目录，不再因真实游戏正在运行而把成功的状态读取误判为测试失败；新增 Windows 真进程回归，覆盖重复启动和 PID 复用。
- 新游戏或读档重置 `Graphics.frame_count` 时，不再把重置前的高帧号旧快照附到命令结果中；等待重置后的新帧快照再返回。
- 姓名输入窗口按真实字符表报告 90 格、10 列和当前字符，不再暴露 VX Ace 基类中误导性的 0/1 默认值。
- JSON 快照现在逐字符转义反斜杠和全部 U+0000–U+001F 控制字符；含 `\\N[1]` 等 RPG Maker 文本控制码的对话不再令桥接状态变成非法 JSON。
- 对话选项按实际光标闭环定位；兼容本作“防误触”脚本把选择窗口初始索引设为 -1 的行为，第一项不再确认无效，后续项也不再错选前一项。
- 已声明选项但仍被前置 `\\!` 文本暂停占用输入时，先推进到选择窗口真正激活再定位目标，避免误报“选择窗口消失”。
- 存档列表的 `slot` 与保存/读档工具统一为 0 起始，并新增 1 起始的 `display_slot`；`Save01` 不再被误传为工具参数 1 而指向 `Save02`。
- 武器、防具和已装备物从 VX Ace 的 `params[0..7]` 读取八项真实属性；不再调用装备对象不存在的 `atk/def` 方法并把数值静默报成 0。
- 地图事件改从 `Game_Event` 的真实 `@event` 数据读取名称和页面；摘要另增当前角色图、图像索引、图块、活动页和擦除状态，详情查询不再把所有页面误报为空。
- 自动寻路把同层级且不可穿透的事件作为真实障碍，并核对目标图块的反向通行；不再规划穿过 NPC/敌人的路线后声称输入完成却停在半路。
- 地图快照随事件坐标、活动页和擦除状态更新；交互会重新追踪移动事件，并仅在对话、战斗、转场或事件状态变化可观测时报告成功，不再把对空地按确认称为完成。
- 战斗行动检测到全队 HP 为 0 时立即推进战败结算，不再在战败文本上耗尽完整超时后误报战斗仍可继续。

## 1.10.0 - 2026-07-26

根据一次完整的真机试玩反馈（11 项问题）修复，全部改动已在真实游戏的 boss 战中复验。

- 修复战斗胜利结算 100% 卡死：桥接此前只挂在 `Scene_Base#update`，而引擎在结算、长演出、转场时走的是内部等待循环，根本不经过该方法。主心跳改挂 `Graphics.update`（每渲染帧都跑），结算画面现在可正常推进回地图。
- 新增 `black_souls_battle_options`（第 36 个工具）：一次列出当前行动者的全部选择——特技/魔法二级菜单的每一条（名称、MP/TP 消耗、当前是否可用、说明）、可用战斗道具及数量、存活敌人目标，每条都带可直接传给 `black_souls_battle_action` 的序号。此前代理只能看到顶层指令，实际只会反复"攻击/恢复"。
- 修复战斗中"打不动"的假成功：MP 不足时引擎只会响一声蜂鸣、光标原地不动，工具却会空等满超时后报告成功，表现为回合数与血量长期不变。现在下手前先校验可用性并直接报错说明差多少 MP/TP，另有一道兜底检查确认光标确实离开了二级菜单。
- 修复 boss 战开场对话导致的 40 秒空等：战斗中有对话时自动推进；出现选项时明确报错要求先用 `black_souls_advance_dialogue` 作答。
- 修复"游戏进程消失"误报：快照文件一时读不到会被当成进程已死，导致把活得好好的游戏判定为崩溃。现在读不到文件不再等于进程死亡，两种情况分别给出不同的结论与建议，并把快照重试窗口放宽到足以跨过一次轮转。
- 修复战斗二级菜单为两列网格导致奇数序号够不到的问题：光标移动改为网格感知（右邻为序号+1，下邻为序号+列数）。
- 修复 `enemy_index` 语义不一致：状态里含阵亡占位，目标窗口只列存活敌人，现在自动换算。
- 选择失败后必定回退到指令窗口，不再把游戏留在半开的技能/道具菜单里（此前有误用全场唯一"女神的祝福"的风险）；无法回退时如实说明当前停在哪个窗口。
- 菜单导航失败信息改为可诊断：报告光标实际走过的每一项，并区分"菜单被游戏关掉"与"没找到该指令"。
- `black_souls_navigate` / `black_souls_interact` 增加场景守卫：不在地图上时直接拒绝。此前在菜单里调用会把方向键当成菜单选择，最接近的一次差点选中"结束游戏"。
- 修复后台唤醒不及时导致的 60-100 秒卡顿：心跳超过 1.2 秒即主动唤醒，等待命令响应期间持续保活。
- 修复标题画面读档时机竞态：窗口在淡入期间可能短暂读不到，现在会等待而不是立即判定菜单已关闭。

## 1.9.1 - 2026-07-26

- 启动后默认自动最小化游戏窗口，防止本机真实键盘输入漏进游戏（可用 minimize_window 参数关闭）；输入通过后台唤醒通道照常工作。

## 1.9.0 - 2026-07-26

- 用真实游戏完成全量校准：存档、读档、战斗、寻路、对话选择首次真机验证通过。
- 菜单导航全部改为闭环：每一步回读游戏内真实光标（窗口符号、存档位索引），不再盲按固定次数；粘性光标和丢帧自动纠正。
- 战斗指令按实测布局重写：攻击/特技/魔法/防御/道具/逃跑六类一次调用直达，敌人目标横向选择，回合结算轮询等待；桥接快照新增真实回合数。
- 存档工具改为"菜单符号定位 → 选槽 → 落盘校验"，以磁盘存档文件的实际变化判定成败。
- 读档工具兼容"选档界面"与"快速继续"两种标题流程；本作菜单内无读档指令时如实报告替代路径。
- 寻路步间等待按实测调整为 18 帧（走一格约 16 帧，连按会被引擎丢弃），多步路径不再丢步。
- 状态读取工具在游戏后台暂停时自动唤醒键盘循环（此前闲置约 60 秒后所有读取会集体报"桥接未就绪"）。
- 修复健康检查在游戏已退出时仍报"一切正常"的误诊；非战斗状态不再返回上一场战斗的残留敌人数据。
- 战斗回合演出期间引擎会暂停处理桥接命令，提交类输入对超时宽容并改为轮询状态恢复。
- 测试脚本把环境变量透传给服务器子进程（此前外部沙箱路径无法生效）；新增真机布局探测工具 `scripts/probe_live.mjs`。

## 1.8.1 - 2026-07-26

- 安装与卸载脚本在改写 Codex 配置前先校验标记块是否成对且唯一，防止孤立标记导致误删无关配置。
- 回滚脚本改按备份文件名内嵌的时间戳排序（复制/同步会破坏文件修改时间），无法识别的文件名退回按修改时间。
- 游戏内桥接的原子写入不再在帧循环内睡眠重试（最坏可冻结约 13 帧），失败改为记录错误并由下一帧自然重试。
- 游戏内桥接的命令收件箱扫描从每帧一次降为每 6 帧一次，减少目录 I/O；对命令响应延迟无感知影响。
- README 的注入命令改为推荐免依赖的 `patch_rvdata2_binary.py`。

## 1.8.0 - 2026-07-26

- 工具从 7 个扩展到 35 个，让 AI 代理可以自主长时间游玩。
- 新增进程控制与自诊断：强制结束游戏、桥接健康检查并给出恢复建议。
- 新增高层动作：自动寻路、走到事件旁交互、一次完成战斗回合、推进对话并选择选项、触发存档与读档、等待游戏条件。
- 新增桥接查询通道：即时读取变量、开关、背包、队伍详情、大范围地图、事件页条件和场景深层状态，不占用帧步进。
- 新增 AI 持久记忆：会话便签、长期知识库（自动淘汰低置信度旧条目）、目标层级、追加式会话日志，全部原子写入且跨上下文重置保留。
- 新增局势摘要工具，一次调用返回场景、队伍、对话、可通行方向和建议动作，减少往返和上下文占用。
- 新增评测框架：`evals/runner.mjs` 内置规则型基线代理，5 个场景文件，`black_souls_eval_status` 检查完成条件，结果写入 `evals/results/`。
- 同步更新 README 工具总览，并将安装与注入步骤直接写入 README（移除失效的 docs 链接）。

## 1.1.1 - 2026-07-23

- 提供 7 个 BLACK SOULS MCP 工具。
- 加入启动代次、进程、帧号、命令编号和损坏快照恢复。
- 支持地图、事件、消息、菜单、队伍和战斗状态。
- 支持白名单键盘动作与连续动作。
- 在不抢占前台的情况下恢复 RPG Maker 后台键盘循环。
- 为安装、卸载和回滚配置备份使用毫秒时间与随机后缀，避免连续操作覆盖备份。
- 将源码整理为不包含游戏、存档和本机运行数据的公开仓库结构。
- 加入中英文入口文档、GitHub CI、问题模板和完整仓库元数据。
- 统一 MCP 工具的结构化错误返回。
- 集成测试会把显式运行目录传给子进程，可真实核对外部游戏副本与存档。
- 限制命令队列、序列步骤和总帧数，并严格校验 RGSS3 命令文件。
- 修正连续动作中等待帧多停一帧的问题。
- 仅在桥接心跳确实停滞时发送后台恢复通知，正常连接不再重复唤醒窗口。
- 启动桥接失败时终止本次新建的游戏进程，避免残留后台进程。
